{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build STCM Package",
            "type": "shell",
            "command": "export PYTHONUSERBASE=\"${PYTHONUSERBASE:-$HOME/.local/stcm_sys_py310}\" && source /opt/ros/humble/setup.bash && colcon build --packages-select stcm --symlink-install",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "detail": "Build the STCM package with ROS sourced on the distro interpreter + isolated PYTHONUSERBASE"
        },
        {
            "label": "Clean Build STCM Package",
            "type": "shell",
            "command": "rm -rf build/ install/ log/ && export PYTHONUSERBASE=\"${PYTHONUSERBASE:-$HOME/.local/stcm_sys_py310}\" && source /opt/ros/humble/setup.bash && colcon build --symlink-install",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": "build",
            "problemMatcher": [],
            "detail": "Clean all build artifacts and rebuild all packages using the system Python runtime + isolated PYTHONUSERBASE"
        },
        {
            "label": "Launch Semantic Mapping",
            "type": "shell",
            "command": "export PYTHONUSERBASE=\"${PYTHONUSERBASE:-$HOME/.local/stcm_sys_py310}\" && source /opt/ros/humble/setup.bash && source install/setup.bash && ros2 launch stcm semantic_mapping.launch.py config_file:=$(ros2 pkg prefix stcm)/share/stcm/config/semantic_mapping_params.yaml",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "detail": "Launch semantic mapping with the config file using the system Python + isolated user base"
        },
        {
            "label": "Benchmark STCM Graph",
            "type": "shell",
            "command": "python3 scripts/experiments/benchmark_stcm_graph.py --prediction output/stcm.json --ground-truth configs/experiments/ground_truth/meeting_stcm_gt.json --match-threshold-m 1.0 --output-json output/stcm_benchmark.json --output-csv output/stcm_benchmark.csv",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "detail": "Score output/stcm.json against the meeting ground-truth STCM graph with same-label F1@1m metrics"
        },
        {
            "label": "Launch GT Annotation GUI",
            "type": "shell",
            "command": "export PYTHONUSERBASE=\"${PYTHONUSERBASE:-$HOME/.local/stcm_sys_py310}\" && export DISPLAY=:77 && export MPLCONFIGDIR=/tmp/matplotlib-${USER} && source /opt/ros/humble/setup.bash && source install/setup.bash && python3 -c \"import importlib.util, sys; sys.exit(0 if importlib.util.find_spec('gradio') else 1)\" || python3 -m pip install --user -r stcm/requirements.txt && export PYTHONUSERBASE=\"${PYTHONUSERBASE:-$HOME/.local/stcm_sys_py310}\" && export DISPLAY=:77 && export MPLCONFIGDIR=/tmp/matplotlib-${USER} && source /opt/ros/humble/setup.bash && source install/setup.bash && python3 scripts/annotate_stcm_gt.py --config stcm/config/semantic_mapping_params.yaml --host 0.0.0.0 --bag /media/dl-box/STREAM1/ranger_recording_20251215_163827_uncompressed --input-json configs/experiments/ground_truth/meeting_stcm_gt.json",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "detail": "Launch the local Gradio app for meeting-room GT inspection with RGB-click SAM + projected-LiDAR object annotation"
        },
        {
            "label": "Show Experiment Results Table",
            "type": "shell",
            "command": "python3 scripts/experiments/results_table.py --summary results/eval/summary.csv --all --include-dry-runs --include-failed",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "detail": "Print all rows from results/eval/summary.csv (including dry-runs and failed benchmarks) as a Markdown table"
        },
        {
            "label": "Run Meeting Full Experiment",
            "type": "shell",
            "command": "env ROS_LOG_DIR=${workspaceFolder}/results/eval/ros_logs python3 scripts/experiments/run_experiment.py --scenario meeting --variant full --skip-bag-hash",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "detail": "Run the meeting scenario with the full variant and keep ROS launch logs under results/eval/ros_logs"
        },
        {
            "label": "Launch Semantic Mapping (with Updater)",
            "type": "shell",
            "command": "export PYTHONUSERBASE=\"${PYTHONUSERBASE:-$HOME/.local/stcm_sys_py310}\" && source /opt/ros/humble/setup.bash && source install/setup.bash && ros2 launch stcm semantic_mapping.launch.py config_file:=$(ros2 pkg prefix stcm)/share/stcm/config/semantic_mapping_params.yaml run_updater:=true",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "detail": "Launch semantic mapping with the updater enabled on the system Python runtime"
        },
        {
            "label": "Run Semantic Map Builder Node",
            "type": "shell",
            "command": "export PYTHONUSERBASE=\"${PYTHONUSERBASE:-$HOME/.local/stcm_sys_py310}\" && source /opt/ros/humble/setup.bash && source install/setup.bash && ros2 run stcm semantic_map_builder --ros-args -p text_prompt:='table . chair . door .' -p graph_output_path:=/tmp/stcm.json",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "detail": "Run semantic map builder node directly with the isolated system Python user base"
        },
        {
            "label": "Download Model Checkpoints",
            "type": "shell",
            "command": "export PYTHONUSERBASE=\"${PYTHONUSERBASE:-$HOME/.local/stcm_sys_py310}\" && source /opt/ros/humble/setup.bash && source install/setup.bash && ros2 run stcm stcm_download_checkpoints",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "detail": "Download all default model checkpoints after sourcing ROS + workspace on system Python"
        },
        {
            "label": "Test GroundingDINO + MobileSAM",
            "type": "shell",
            "command": "export PYTHONUSERBASE=\"${PYTHONUSERBASE:-$HOME/.local/stcm_sys_py310}\" && source /opt/ros/humble/setup.bash && source install/setup.bash && python3 stcm/test/test_gdino_sam.py stcm/imgs/image007.png",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "detail": "Test the perception pipeline on a sample image with the system Python user base"
        },
        {
            "label": "Test Depth Anything",
            "type": "shell",
            "command": "export PYTHONUSERBASE=\"${PYTHONUSERBASE:-$HOME/.local/stcm_sys_py310}\" && source /opt/ros/humble/setup.bash && source install/setup.bash && python3 stcm/test/test_depth_anything.py stcm/imgs/image00.png",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "detail": "Test depth estimation on sample image"
        },
        {
            "label": "Run Semantic Graph Simulator",
            "type": "shell",
            "command": "export PYTHONUSERBASE=\"${PYTHONUSERBASE:-$HOME/.local/stcm_sys_py310}\" && export DISPLAY=:77 && source /opt/ros/humble/setup.bash && source install/setup.bash && ros2 run stcm_planner semantic_graph_simulator --ros-args -p graph_path:=./output/stcm.json -p model:=qwen2.5:14b -p run_mode:=use_tools",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "detail": "Run semantic graph simulator with Qwen 2.5 14B (Ollama) in use_tools mode on display :77"
        },
        {
            "label": "Launch RViz2 for Semantic Graph Simulator",
            "type": "shell",
            "command": "export DISPLAY=:77 && source /opt/ros/humble/setup.bash && source install/setup.bash && rviz2 -d ${workspaceFolder}/stcm_planner/rviz/semantic_graph_simulator.rviz",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "detail": "Launch RViz2 with semantic graph simulator visualization config on display :77"
        },
        {
            "label": "Run Language Query Publisher",
            "type": "shell",
            "command": "export PYTHONUSERBASE=\"${PYTHONUSERBASE:-$HOME/.local/stcm_sys_py310}\" && source /opt/ros/humble/setup.bash && source install/setup.bash && ros2 run stcm_planner language_query_publisher",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "detail": "Run language query publisher node using the system Python runtime + isolated user base"
        },
        {
            "label": "Install Qwen Models",
            "type": "shell",
            "command": "bash scripts/install_qwen.sh",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "detail": "Run the Qwen installation script to install Ollama and pull Qwen models"
        },
        {
            "label": "Check Ollama Status",
            "type": "shell",
            "command": "if pgrep -x 'ollama' > /dev/null; then echo '✓ Ollama is running (PID: '$(pgrep -x ollama)')'; ollama --version 2>/dev/null || echo 'Version: unknown'; else echo '✗ Ollama is not running. Start it with: ollama serve'; fi",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "detail": "Check if Ollama service is running and display version"
        },
        {
            "label": "List Qwen Models",
            "type": "shell",
            "command": "if command -v ollama >/dev/null 2>&1; then echo 'Installed Qwen models:' && ollama list | grep -E '(qwen|deepseek-r1|NAME)' || echo 'No Qwen models found'; else echo '✗ Ollama not installed. Run: Install Qwen Models task'; fi",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "detail": "List all installed Qwen and DeepSeek-R1 models"
        },
        {
            "label": "Start Ollama Server",
            "type": "shell",
            "command": "if pgrep -x 'ollama' > /dev/null; then echo '✓ Ollama is already running (PID: '$(pgrep -x ollama)')'; else echo 'Starting Ollama server...'; nohup ollama serve > /tmp/ollama.log 2>&1 & sleep 2 && echo '✓ Ollama started (PID: '$(pgrep -x ollama)') - Logs: /tmp/ollama.log'; fi",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "detail": "Start Ollama server in background if not already running"
        },
        {
            "label": "Test Qwen Model (Interactive)",
            "type": "shell",
            "command": "MODEL=\"${input:qwenModel}\" && if command -v ollama >/dev/null 2>&1; then echo \"Testing $MODEL...\" && ollama run $MODEL 'Say hello in one sentence.'; else echo '✗ Ollama not installed'; fi",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "detail": "Run a quick test query against a selected Qwen model interactively"
        },
        {
            "label": "Pull Qwen Model",
            "type": "shell",
            "command": "MODEL=\"${input:qwenModel}\" && if command -v ollama >/dev/null 2>&1; then echo \"Pulling $MODEL...\" && ollama pull $MODEL; else echo '✗ Ollama not installed. Run: Install Qwen Models task'; fi",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "detail": "Pull a specific Qwen model using Ollama"
        },
        {
            "label": "Build i-gng Package",
            "type": "shell",
            "command": "export PYTHONUSERBASE=\"${PYTHONUSERBASE:-$HOME/.local/stcm_sys_py310}\" && python3 -m pip install --user pybind11 setuptools wheel && cd i-gng && python3 -m pip install --user -e . && cd .. && echo '✓ i-gng built and installed successfully'",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [],
            "detail": "Build and install i-gng as a standalone Python package for GNG instance management features"
        }
    ],
    "inputs": [
        {
            "id": "qwenModel",
            "type": "pickString",
            "description": "Select Qwen model",
            "options": [
                "deepseek-r1:7b",
                "qwen2.5:7b",
                "qwen2.5:14b",
                "qwen2.5:32b",
                "qwen2.5:72b"
            ],
            "default": "qwen2.5:14b"
        }
    ]
}
