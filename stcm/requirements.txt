# numerics
# numpy<2.0.0 is installed by setup_stcm_env.sh (ros2_numpy built from source supports numpy 1.24+)
networkx
tqdm

# vision & geometry
opencv-python
open3d
scikit-image
shapely
Pillow
matplotlib

# segmentation / helpers
# NOTE: mobile-sam is installed separately via setup_stcm_env.sh from GitHub
# pip install git+https://github.com/ChaoningZhang/MobileSAM.git
supervision>=0.18.0  # Relax to allow GroundingDINO's newer supervision

# HF ecosystem (relax to allow GroundingDINO's dependencies)
transformers>=4.44.0
huggingface-hub>=0.25.0
ftfy
regex

# infra / utils
requests
PyYAML
packaging
chardet
absl-py

# ROS helper
# ros2_numpy is built from Box-Robotics source as a git submodule (see ros2_numpy/ directory)
transforms3d>=0.4.1  # Override Ubuntu's ROS dependency to avoid the removed np.float alias

# NOTE: torch, torchvision, and groundingdino are installed separately
# See README.md for proper installation instructions
